# TEST-P4-003: Write integration tests for all API endpoints

- **Success Criteria**:
  - Integration tests for all CRUD endpoints: accounts, categories, transactions, debts, tax, templates, profile, backup
  - Tests verify: correct HTTP status codes, response payloads, validation errors, auth guards
  - Tests use isolated test database
  - All tests pass
- **Dependencies**: All API tasks
